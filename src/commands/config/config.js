const Discord = require(`discord.js-light`);
const log = require(`../../modules/log.js`);

module.exports = {
    name: `config`,
    description: `Configure Andrew! for your server`,
    guildOnly: true,
    aliases: [`start`, `init`, `configure`, `setup`],
    async execute (client, message, args) {

    },
}